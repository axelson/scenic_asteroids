<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>PlayWeb · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body>
    <header>
      <section class="container">
        <nav role="navigation">
          <ul>
            <%= if assigns[:username] do %>
              <li>
                Hi <%= @username %>
                <%= link "logout", to: "/logout", method: "post" %>
              </li>
              <li>
                <%= link "play", to: "/play" %>
              </li>
            <% end %>
          </ul>
        </nav>
        <a href="/" class="phx-logo">
          <img src="<%= Routes.static_path(@conn, "/images/logo.png") %>" alt="Scenic Asteroids Logo"/>
        </a>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>

    <%= if assigns[:username] do %>
      <script>
       var SocketExports = <%= raw %{token: @user_token} |> Jason.encode! %>;
      </script>
    <% end %>
    <script src="<%= Routes.static_path(@conn, "/js/phaser-arcade-physics.min.js") %>"></script>
    <script
			src="<%= Routes.static_path(@conn, "/js/jquery-3.4.1.slim.min.js") %>"
			integrity="sha256-pasqAKBDmFT4eHoN2ndd6lN370kFiGUFyTiUHWhU7k8="
			crossorigin="anonymous"></script>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
